# mock-api
front end mock API data

## 安装

### 导入sql

	git clone git://github.com/cqb325/mock-api.git

	$ cd mock-api

	//install node modules
	$ npm install

导入`sql`文件目录地址`mock-api/sql` 数据库为`mysql`或`mariaDB`

	//start mock-api service
	$ npm start

浏览器中打开 `http://127.0.0.1:8415`
